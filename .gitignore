# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# dbt
dbt_ski_resort/target/
dbt_ski_resort/dbt_packages/
dbt_ski_resort/dbt_modules/
dbt_ski_resort/logs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Snowflake secrets (never commit keys or credentials)
*.p8
*.pem
connections.toml

# Project generated artifacts
agent-evaluation/results/
agents/snapshots/
semantic-views/snapshots/
.plans/

# Agent versioning — local snapshots and WIP framework docs
/snapshots/
/framework/
/agent_optimization/

# Local test/debug scripts at repo root (but do NOT ignore tests/ directory)
/test_*.py
/audit_*.py

# Misc
*.log
.env
.env.act
.secrets.act
.snowflake/

# Local conversation/chat artifacts (Cortex Code transcripts, scratch notes)
AgentMangementThread*.md
*Thread.md
/scratch/
/.coco/
/.cortex-code/

# REQ-014 — embedded Cortex Code chat (work-in-progress, local only)
/cortex-code-agent/
/streamlit/
# Local/reference assets. These may exist in Jeremy's working copy but should
# not be added as new public repo content. Runtime config in /environments/ is
# still tracked until CI no longer reads those files directly.
/docs/
/models/
/requirements/
/reqs/
/old/
AGENTS.md
