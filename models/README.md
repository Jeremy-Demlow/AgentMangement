# Models

Data model definitions. One file per logical entity or domain area.

## How to Use
1. Copy `_TEMPLATE_model.md` and rename (e.g., `orders_model.md`)
2. Document the schema, relationships, and business logic
3. Keep `docs/data_dictionary.md` in sync with models defined here
